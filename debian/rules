#!/usr/bin/make -f
# -*- makefile -*-

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

export DEB_BUILD_HARDENING=1

%:
	dh $@ 

override_dh_auto_install:
	dh_auto_install
	rmdir debian/pushpin/var/run/pushpin
	rmdir debian/pushpin/var/run
