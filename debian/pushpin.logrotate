/var/log/pushpin/*.log {
        daily
        missingok
        rotate 7
	sharedscripts
        postrotate
                if [ -e /var/run/pushpin.pid ]; then
			kill -HUP $(cat /var/run/pushpin.pid)
                fi
        endscript
        compress
        notifempty
}
